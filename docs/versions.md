# Documentation Versions

Use the version selector in the header to switch between releases.

How versions are published:
- Tag a release like `v0.1.2` and push the tag.
- GitHub Actions builds and publishes that version.
- The `latest` alias is updated to the most recent tag.

If you are viewing a non-latest version, consider switching to `latest` for current guidance.
